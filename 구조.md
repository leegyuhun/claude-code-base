● claude-code-base/ (Delphi 2007 프로젝트)
  │
  ├── PRD.md                          ← [사용자 작성] 프로젝트 요구사항 문서
  │                                      (목적, 기능, 기술 스택, MVP 기준, 제약 조건)
  │
  ├── STATUS.md                       ← [공유 상태 파일] 현재 PHASE 기록
  │
  ├── build.bat                       ← Delphi dcc32 빌드 스크립트 (debug/release)
  ├── run_tests.bat                   ← DUnit 테스트 실행 스크립트
  │
  ├── Source/                         ← Delphi 소스 코드
  │   ├── Forms/                      ← 폼 파일 (.pas + .dfm 쌍)
  │   ├── Units/                      ← 비주얼 아닌 비즈니스 로직 유닛 (.pas)
  │   └── DataModules/                ← 데이터 모듈 (.pas + .dfm 쌍)
  │
  ├── Lib/                            ← 외부 라이브러리 .dcu (gitignored)
  │
  ├── Output/
  │   ├── Debug/                      ← 디버그 빌드 .exe (gitignored)
  │   └── Release/                    ← 릴리스 빌드 .exe (gitignored)
  │
  ├── Tests/                          ← DUnit 테스트
  │   ├── Source/                     ← 테스트 유닛 (.pas)
  │   └── TestRunner.dpr              ← 테스트 실행 프로젝트 (자동 생성)
  │
  ├── Resources/                      ← 리소스 파일 (.res, .rc, .ico, .bmp)
  │
  ├── docs/
  │   └── dev-process.md              ← 전체 개발 프로세스 정책 문서
  │
  ├── sprints/                        ← [자동 생성] Orchestrator 초기화 시 생성
  │   ├── ROADMAP.md                  ← 전체 스프린트 로드맵
  │   ├── sprint-01/
  │   │   ├── GOAL.md                 ← 구현 명세서 (Planner 생성)
  │   │   ├── DONE.md                 ← 완료 보고서 (Validator 생성)
  │   │   └── OUT_OF_SCOPE.md         ← 범위 외 발견사항
  │   └── sprint-02/
  │       └── ...
  │
  ├── plan.md                         ← [자동 생성] PRD 분석 결과
  ├── CLAUDE.md                       ← [자동 생성] 기술 스택, 빌드 명령, 코딩 원칙
  │
  └── .claude/
      ├── settings.json               ← Claude Code 권한 설정 (dcc32 포함)
      │
      ├── agents/
      │   ├── orchestrator.md         ← PHASE 1~4.5: PRD 분석 → plan.md → ROADMAP.md
      │   ├── planner.md              ← PHASE 5: GOAL.md 작성
      │   ├── implementer.md          ← PHASE 6: .pas/.dfm 구현
      │   ├── validator.md            ← PHASE 7~10: dcc32 빌드 검증 → DUnit → PR
      │   ├── hotfix-close.md         ← 핫픽스 마무리
      │   └── deploy-prod.md          ← 릴리스 배포
      │
      ├── commands/
      │   └── sprint-dev.md           ← /sprint-dev {스프린트명}
      │
      ├── rules/
      │   ├── sprint-workflow.md      ← 스프린트/핫픽스 워크플로우 규칙
      │   └── coding-principles.md    ← Delphi/Pascal 코딩 원칙
      │
      └── skills/
          ├── status.md               ← /status
          ├── next.md                 ← /next
          ├── rollback.md             ← /rollback
          ├── sprint-log.md           ← /sprint-log
          └── debt.md                 ← /debt
