● ysr/ (Delphi 2007 프로젝트 — 루프 엔지니어링 적용)
  │
  ├── docs/PRD_#이슈.md               ← [사용자 / `/prd`] 이슈별 요구사항 입력 문서
  │                                      (목적, 기능, 기술 스택, MVP 기준, 제약 조건, 검증 계약)
  │
  ├── VISION.md                       ← [신규·불변] 시스템 방향 + 절대 도메인 제약 (루프 수정 금지)
  ├── AGENTS.md                       ← [신규·규칙] Maker/Checker 주입 작업 규칙
  │                                      (CP949 인코딩 · Sybase/PG SQL · 코딩 · 트랙 · 브랜치 · 리뷰)
  │
  ├── build.bat                       ← rsvars.bat + msbuild 빌드 스크립트 (debug/release)
  ├── run_tests.bat                   ← DUnit 테스트 실행 스크립트
  ├── Tests/
  │   ├── Source/                     ← 테스트 유닛 (.pas)
  │   └── TestRunner.dpr              ← 테스트 실행 프로젝트 (자동 생성)
  │
  ├── CLAUDE.md                       ← [자동 생성] 기술 스택, 빌드 명령, 코딩 원칙
  ├── CHANGELOG.md                    ← [자동 생성] 전체 변경 이력 (사람 게이트 PHASE 9에서 업데이트)
  │
  ├── workspace/                      ← [자동 생성] 이슈별 산출물 루트 (이슈별 격리)
  │   └── #207500/
  │       ├── STATUS.md               ← 파이프라인 포인터 (PHASE / TRACK / 스프린트) — KEEP 커맨드 호환
  │       ├── PROGRESS.md             ← [신규] 루프 이터레이션 상태 (시도 / 결과 / 다음 계획)
  │       ├── plan.md                 ← PRD 분석 결과 (Orchestrator)
  │       └── sprints/
  │           ├── ROADMAP.md          ← 전체 스프린트 로드맵
  │           ├── TECH_DEBT.md        ← 누적 기술 부채 (사람 게이트 PHASE 9에서 집계)
  │           ├── sprint-01/
  │           │   ├── GOAL.md         ← 구현 명세 + 검증 계약 (Planner) — 루프 종료조건의 기준
  │           │   ├── DONE.md         ← 완료 보고서 (PHASE 9)
  │           │   ├── FEEDBACK.md     ← 검증 실패 시 타겟 수정 지시 (루프 재진입용, 선택)
  │           │   └── OUT_OF_SCOPE.md ← 범위 외 발견사항
  │           └── sprint-02/
  │               └── ...
  │
  ├── .githooks/
  │   └── commit-msg                  ← 커밋 메시지 형식 검증 hook (YSR 컨벤션)
  │
  └── .claude/
      ├── settings.json               ← 권한 + PreToolUse 훅 (CP949 차단·bash-guard = 루프 안전장치 게이트)
      ├── hooks/
      │   └── pretooluse-bash-guard.sh ← force push·master·reset 차단
      │
      ├── loops/                      ← [신규] 루프 설계 (5블록 단일 소스)
      │   ├── loop-sprint/LOOP.md     ← 구현 ⇄ 검증 루프 (Maker-Checker)
      │   ├── loop-build-fix/LOOP.md  ← 빌드 복구 루프
      │   └── MIGRATION.md            ← 하네스→루프 전환 기록 + hooks 매핑 + 롤백
      │
      ├── agents/
      │   ├── orchestrator.md         ← PHASE 1~4.5: PRD 분석 → plan.md → ROADMAP.md
      │   ├── planner.md              ← PHASE 5: GOAL.md 작성
      │   ├── implementer.md          ← loop-sprint의 Maker (구현 생성)
      │   ├── validator.md            ← 자동검증 = loop Checker로 흡수 / PHASE 8~10 = 사람 게이트
      │   ├── deploy-prod.md          ← 릴리스 배포
      │   └── commit-writer.md        ← 커밋 메시지 작성 (PHASE 9에서 호출)
      │
      ├── commands/
      │   ├── loop-sprint.md          ← [신규] /loop-sprint — 구현⇄검증 루프 진입점
      │   ├── loop-build-fix.md       ← [신규] /loop-build-fix — 빌드 복구 루프 진입점
      │   ├── prd.md                  ← /prd — PRD.md 생성
      │   ├── status.md               ← /status — 현재 상태 요약
      │   ├── next.md                 ← /next — 다음 단계 안내
      │   ├── rollback.md             ← /rollback — PHASE 롤백
      │   ├── sprint-log.md           ← /sprint-log — 스프린트 종합 요약
      │   ├── debt.md                 ← /debt — Tech Debt 보고
      │   ├── branch.md               ← /branch — 브랜치 생성
      │   └── resolve.md              ← /resolve {이슈번호} — Redmine Resolved 처리
      │
      ├── skills/                     ← 루프가 각 스텝에서 호출 (신규 변환 없음 — 기존 체계 재사용)
      │   ├── redmine/                  ← Redmine 이슈 조회
      │   ├── commit-format/            ← YSR 커밋 메시지 형식
      │   ├── subagent-driven-development/   ← Maker 디스패치 절차 + 2단계 리뷰
      │   ├── systematic-debugging/     ← 근본 원인 조사 (loop-build-fix Maker 의무)
      │   ├── verification-before-completion/ ← 완료 선언 전 증거 확보
      │   ├── requesting-code-review/   ← Checker C3 코드 리뷰 디스패치
      │   └── writing-plans/            ← 계획 문서 자기검증
      │
      ├── rules/
      │   ├── encoding-critical.md    ← CP949 인코딩 보호 (항상 활성화)
      │   ├── coding-principles.md    ← Delphi/Pascal 코딩 원칙 (paths 자동 로드)
      │   ├── delphi2007-patterns.md  ← Delphi 2007 구현 패턴 모음 (명시적 Read 필요)
      │   ├── active-issue.md         ← 워크스페이스 해석 규칙 (항상 활성화)
      │   └── pitfalls-index.md       ← 함정 인덱스 (본문은 refs/pitfalls.md)
      │
      └── _harness_backup/            ← [신규] 구 하네스 백업 (롤백용, MIGRATION.md §5 참조)
          ├── sprint-dev.md           ← 구 /sprint-dev (→ loop-sprint로 대체)
          ├── dev-process.md          ← (→ LOOP.md 파이프라인 + AGENTS.md 규칙으로 흡수)
          └── sprint-workflow.md      ← (→ AGENTS.md 트랙·체크박스로 흡수)
