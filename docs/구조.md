● claude-code-base/ (Python + React + TypeScript 프로젝트 기준)
  │
  ├── docs/PRD.md                          ← [사용자 작성] 프로젝트 요구사항 문서
  │                                           (목적, 기능, 기술 스택, MVP 기준, 제약 조건)
  │
  ├── docs/STATUS.md                       ← [공유 상태 파일] 현재 PHASE 기록
  │
  ├── plan.md                              ← [자동 생성] PRD 분석 결과
  ├── CLAUDE.md                            ← [자동/수동 생성] 기술 스택, 빌드 명령, 코딩 원칙
  ├── CHANGELOG.md                         ← [자동 생성] 전체 변경 이력 (Validator 생성)
  │
  ├── backend/                             ← Python 백엔드 (FastAPI 등)
  │   ├── app/
  │   │   ├── main.py                      ← FastAPI 앱 엔트리포인트
  │   │   ├── api/
  │   │   │   └── v1/
  │   │   │       ├── routes/              ← APIRouter 단위 파일 (users.py, items.py)
  │   │   │       └── dependencies.py      ← 공통 Depends() 의존성
  │   │   ├── core/                        ← config.py, security.py
  │   │   ├── models/                      ← SQLAlchemy 모델 (단수형)
  │   │   ├── schemas/                     ← Pydantic 스키마 (Request/Response)
  │   │   ├── services/                    ← 비즈니스 로직
  │   │   ├── repositories/               ← DB 접근 로직
  │   │   └── db/                          ← session.py, base.py
  │   ├── tests/
  │   │   ├── unit/                        ← pytest 단위 테스트
  │   │   ├── integration/                 ← httpx.AsyncClient API 테스트
  │   │   └── conftest.py                  ← fixture (DB, client, auth token)
  │   ├── alembic/                         ← DB 마이그레이션 파일
  │   ├── pyproject.toml / requirements.txt
  │   └── .env.example                     ← 환경변수 키 목록 (값 없음, 커밋 O)
  │
  ├── frontend/                            ← React + TypeScript (Vite)
  │   ├── src/
  │   │   ├── pages/                       ← 라우트 단위 페이지 컴포넌트
  │   │   ├── components/
  │   │   │   ├── common/                  ← Button, Input, Modal 등 범용
  │   │   │   └── feature/                 ← 기능별 컴포넌트
  │   │   ├── hooks/                       ← 커스텀 훅 (use 접두사)
  │   │   ├── services/                    ← API 호출 함수 (axios 래핑)
  │   │   ├── store/                       ← 전역 상태 (Zustand 등)
  │   │   ├── types/                       ← 공용 TypeScript 타입/인터페이스
  │   │   ├── lib/                         ← axios 인스턴스 등 외부 설정
  │   │   └── utils/                       ← 순수 유틸리티 함수
  │   ├── tests/                           ← Vitest 단위 테스트
  │   ├── package.json
  │   ├── tsconfig.json
  │   └── vite.config.ts
  │
  ├── e2e/                                 ← Playwright E2E 테스트 (별도 Node.js 모듈)
  │   ├── playwright.config.ts             ← baseURL, timeout, retry, webServer 설정
  │   ├── package.json
  │   └── tests/
  │       └── *.spec.ts                    ← 핵심 사용자 시나리오
  │
  ├── docker-compose.yml                   ← 로컬 개발용 (DB, Redis 등)
  │
  ├── .github/
  │   └── workflows/
  │       └── ci.yml                       ← CI/CD 파이프라인 (pytest→vitest→E2E→배포)
  │
  ├── sprints/                             ← [자동 생성] Orchestrator 초기화 시 생성
  │   ├── ROADMAP.md                       ← 전체 스프린트 로드맵
  │   ├── TECH_DEBT.md                     ← 누적 기술 부채 중앙 관리
  │   ├── sprint-01/
  │   │   ├── GOAL.md                      ← 구현 명세서 (Planner 생성)
  │   │   ├── DONE.md                      ← 완료 보고서 (Validator 생성)
  │   │   └── OUT_OF_SCOPE.md              ← 범위 외 발견사항
  │   └── sprint-02/
  │       └── ...
  │
  └── .claude/
      ├── settings.json                    ← Claude Code 권한 설정
      │
      ├── agents/
      │   ├── orchestrator.md              ← PHASE 1~4.5: PRD 분석 → plan.md → ROADMAP.md
      │   ├── planner.md                   ← PHASE 5: GOAL.md 작성 (Python/TS 산출물 기준)
      │   ├── implementer.md               ← PHASE 6: 구현 (Python/React 패턴 준수)
      │   ├── validator.md                 ← PHASE 7~10: pytest→vitest→E2E→PR
      │   ├── hotfix-close.md              ← 핫픽스 마무리
      │   └── deploy-prod.md               ← 프로덕션 배포 (CI/CD 연동)
      │
      ├── commands/
      │   ├── sprint-dev.md                ← /sprint-dev {스프린트명}
      │   ├── status.md                    ← /status — 현재 상태 요약
      │   ├── next.md                      ← /next — 다음 에이전트 안내
      │   ├── rollback.md                  ← /rollback — PHASE 롤백
      │   ├── sprint-log.md                ← /sprint-log — 스프린트 종합 요약
      │   ├── debt.md                      ← /debt — Tech Debt 보고
      │   └── review.md                    ← /review — PR 코드 리뷰
      │
      └── rules/
          ├── sprint-workflow.md           ← 스프린트/핫픽스 워크플로우 규칙
          ├── coding-principles.md         ← Python + React/TS 코딩 원칙
          ├── cicd.md                      ← CI/CD 파이프라인 규칙 (GitHub Actions)
          └── dev-process.md               ← 개발 프로세스 정책 문서
