● {project}/ (대상 프로젝트 루트)
  │
  ├── docs/
  │   ├── PRD_{ACTIVE_ISSUE}.md       ← [/prd 생성] 이슈별 요구사항 문서
  │   ├── STATUS.md                   ← [템플릿] 실제 상태는 workspace/ 하위에 생성됨
  │   ├── 구조.md                     ← 이 문서
  │   ├── 프로세스.md                 ← 전체 개발 흐름 (텍스트)
  │   └── 흐름도.html                 ← 전체 개발 흐름 (다이어그램 — 브라우저로 열기)
  │
  ├── workspace/                      ← [자동 생성] 이슈별 산출물 격리
  │   └── {ACTIVE_ISSUE}/             ← #208801 또는 임시 ID (exp_login 등)
  │       ├── STATUS.md               ← [공유 상태 파일] PHASE / TRACK / LOOP
  │       ├── plan.md                 ← PRD 분석 결과 (Orchestrator)
  │       ├── DONE.md                 ← TRACK=defect일 때 루트에 직접 생성
  │       ├── COMMIT_MESSAGE.md       ← commit-writer 산출물
  │       ├── .loop/                  ← [루프 층] 카운터·이력 (커밋 대상 아님)
  │       │   ├── counter.json        ← scope별 시도 횟수·벽시계·실패 시그니처
  │       │   └── attempts.log        ← 실패한 시도 이력 (한 줄 압축)
  │       └── sprints/                ← TRACK=sprint일 때만
  │           ├── ROADMAP.md          ← 전체 스프린트 로드맵
  │           ├── TECH_DEBT.md        ← 누적 기술 부채 (Validator가 집계)
  │           └── sprint-NN/
  │               ├── GOAL.md         ← 구현 명세서 + 검증 계약 (Planner)
  │               ├── DONE.md         ← 완료 보고서 (Validator)
  │               ├── FEEDBACK.md     ← 검증 실패 시 타겟 수정 지시 (최신본만)
  │               └── OUT_OF_SCOPE.md ← 범위 외 발견사항
  │
  ├── setup_claude.bat                ← 최초 설치 (git hook, settings.local, .gitignore)
  │
  ├── CLAUDE.md                       ← [자동 생성] 기술 스택, 빌드 명령, 코딩 원칙
  ├── CHANGELOG.md                    ← [자동 생성] 전체 변경 이력 (Validator)
  │
  ├── .githooks/
  │   └── commit-msg                  ← 커밋 메시지 형식 검증 ({타입}: {설명})
  │
  └── .claude/
      ├── ACTIVE_ISSUE                ← 현재 작업 이슈 포인터 (모든 경로 해석의 시작점)
      ├── settings.json               ← 권한 + 훅 배선 (팀 공유, 형상관리 대상)
      ├── settings.local.json         ← 토큰·개인 설정 (.gitignore)
      ├── harness.json                ← 빌드·테스트·fast_check 명령 어댑터 (팀 공유, PHASE 4.5가 채움)
      │
      ├── hooks/                      ← [루프 층] 전부 python — 셸 기동 비용과 따옴표 파싱 회피
      │   ├── pretooluse-bash-guard.py    ← 위험 명령 6규칙 (push/reset/cd/브랜치명)
      │   ├── posttooluse-fast-gate.py    ← 짧은 루프: 소스 변경 시 fast_check 실행
      │   ├── stop-loop-gate.py           ← 긴 루프: PHASE 6 빌드 / PHASE 7 검증계약
      │   └── precompact-preserve.py      ← 컴팩션 시 작업 좌표 보존
      │
      ├── scripts/
      │   ├── gate_harness.py         ← [층 A] 하네스 무결성 (JSON·문법·참조·훅경로)
      │   ├── loop_state.py           ← [루프 층] 카운터·벽시계·헛돌기 감지
      │   ├── policy.py               ← 작업 정책 (PAUSE 1회 인터뷰 결과)
      │   └── harness_config.py       ← harness.json 로더·실행기 (show / run build|test|fast_check)
      │
      ├── tests/                      ← 하네스 회귀 테스트
      │   ├── run_all.py              ← 전체 실행. 훅·scripts 수정 후 수동 실행
      │   ├── _harness.py             ← 공통 헬퍼 (격리 레포 생성, 결과 집계)
      │   ├── test_guards.py          ← PreToolUse Bash 가드
      │   ├── test_fast_gate.py       ← PostToolUse 빠른 게이트
      │   ├── test_loop_state.py      ← 카운터·상한·헛돌기
      │   ├── test_stop_gate.py       ← Stop 훅 (PHASE 게이팅·부재 구분·탈출)
      │   └── test_policy.py          ← 정책 값 검증·저장
      │
      ├── loop-policy.json            ← 작업 정책 (gitignore, /prd가 1회 생성)
      │
      ├── agents/
      │   ├── orchestrator.md         ← PHASE 1~4.5: PRD 분석 → plan.md → ROADMAP.md
      │   ├── planner.md              ← PHASE 5: GOAL.md 작성
      │   ├── implementer.md          ← PHASE 6: 소스 구현 (sprint-dev 래퍼)
      │   ├── validator.md            ← PHASE 7~10: 빌드 검증 → 수동 테스트 → PR/MR 안내
      │   ├── deploy-prod.md          ← 릴리스 배포
      │   └── commit-writer.md        ← 커밋 메시지 작성 (Validator가 호출)
      │
      ├── commands/
      │   ├── prd.md                  ← /prd {이슈ID} — PRD 생성 + 브랜치 + TRACK 판정
      │   ├── sprint-dev.md           ← /sprint-dev — 구현 오케스트레이터 (3-STEP 리뷰)
      │   ├── next.md                 ← /next — 루프 중단 확인 후 다음 단계 안내
      │   ├── status.md               ← /status — 현재 상태 요약
      │   ├── rollback.md             ← /rollback — PHASE 롤백
      │   ├── sprint-log.md           ← /sprint-log — 스프린트 종합 요약
      │   ├── debt.md                 ← /debt — Tech Debt 보고
      │   └── branch.md               ← /branch — 브랜치 생성
      │
      ├── skills/
      │   ├── commit-format/              ← 커밋 메시지 형식
      │   ├── subagent-driven-development/ ← 항목별 fresh subagent + 2단계 리뷰
      │   ├── requesting-code-review/     ← code-reviewer 디스패치 템플릿
      │   ├── systematic-debugging/       ← 가설 → 격리 → 수정 절차
      │   ├── verification-before-completion/ ← 완료 선언 전 증거 확보
      │   └── writing-plans/              ← 계획 문서 작성 기준
      │
      ├── rules/
      │   ├── active-issue.md         ← ACTIVE_ISSUE / WORKSPACE_DIR 해석 규칙
      │   ├── dev-process.md          ← 개발 프로세스 정책 + 브랜치 명명 규칙
      │   ├── sprint-workflow.md      ← 트랙 정의(Sprint/Defect) + 보완 규칙
      │   ├── coding-principles.md    ← 언어 무관 코딩 원칙
      │   └── pitfalls-index.md       ← 함정 인덱스 (본문은 refs/pitfalls.md)
      │
      ├── templates/
      │   ├── goal-format.md          ← GOAL.md 작성 양식 + 자기검증 체크리스트
      │   └── prd-format.md           ← PRD 작성 양식
      │
      └── refs/
          └── pitfalls.md             ← 함정 본문 (4단: 증상·원인·해결·다음 체크)


─────────────────────────────────────────────────────────────────────
루프 층 요약 — 어떤 훅이 언제 도는가
─────────────────────────────────────────────────────────────────────

  PreToolUse   Bash 실행 직전   위험 명령 6규칙 차단

  PostToolUse  도구 실행 직후   [짧은 루프] source_globs에 걸리는 파일이 바뀌면 fast_check 실행
                                실패하면 exit 2로 즉시 교정 요구

  Stop         턴 종료 시점     [긴 루프] 층 A 하네스 무결성은 항상 검사
                                PHASE 6 → 빌드 / PHASE 7 → 검증계약 잔여
                                실패 시 exit 2로 종료를 막고 행동 지시를 stderr로 전달
                                PHASE 8(수동 테스트 대기)은 무조건 통과
                                검증기 부재(harness.json cmd 비어 있음)는 실패가 아닌 통과

  PreCompact   컴팩션 직전      작업 좌표만 남김 (내용이 아니라 경로)

  상한에 걸리면 STATUS.md에 LOOP=halted 기록 → /next가 사유와 이력을 안내
  재개: python .claude/scripts/loop_state.py clear-halt
