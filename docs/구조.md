● ysr/ (Delphi 2007 프로젝트)
  │
  ├── PRD.md                          ← [사용자 작성] 프로젝트 요구사항 문서
  │                                      (목적, 기능, 기술 스택, MVP 기준, 제약 조건)
  │
  ├── STATUS.md                       ← [공유 상태 파일] 현재 PHASE 기록
  │
  ├── build.bat                       ← rsvars.bat + msbuild 빌드 스크립트 (debug/release)
  ├── run_tests.bat                   ← DUnit 테스트 실행 스크립트
  ├── Tests/                          ← DUnit 테스트
  │   ├── Source/                     ← 테스트 유닛 (.pas)
  │   └── TestRunner.dpr              ← 테스트 실행 프로젝트 (자동 생성)
  │
  ├── sprints/                        ← [자동 생성] Orchestrator 초기화 시 생성
  │   ├── ROADMAP.md                  ← 전체 스프린트 로드맵
  │   ├── TECH_DEBT.md               ← 누적 기술 부채 (Validator가 자동 집계)
  │   ├── sprint-01/
  │   │   ├── GOAL.md                 ← 구현 명세서 (Planner 생성)
  │   │   ├── DONE.md                 ← 완료 보고서 (Validator 생성)
  │   │   └── OUT_OF_SCOPE.md         ← 범위 외 발견사항
  │   └── sprint-02/
  │       └── ...
  │
  ├── plan.md                         ← [자동 생성] PRD 분석 결과
  ├── CLAUDE.md                       ← [자동 생성] 기술 스택, 빌드 명령, 코딩 원칙
  ├── CHANGELOG.md                    ← [자동 생성] 전체 변경 이력 (Validator가 스프린트 종료 시 업데이트)
  │
  ├── .githooks/
  │   └── commit-msg              ← 커밋 메시지 형식 검증 hook (YSR 컨벤션)
  │
  └── .claude/
      ├── settings.json               ← Claude Code 권한 설정
      ├── hooks/
      │   └── pretooluse-bash-guard.sh ← bash 명령 가드 훅
      │
      ├── agents/
      │   ├── orchestrator.md         ← PHASE 1~4.5: PRD 분석 → plan.md → ROADMAP.md
      │   ├── planner.md              ← PHASE 5: GOAL.md 작성
      │   ├── implementer.md          ← PHASE 6: .pas/.dfm 구현
      │   ├── validator.md            ← PHASE 7~10: 빌드 검증 → DUnit → PR
      │   ├── deploy-prod.md          ← 릴리스 배포
      │   └── commit-writer.md        ← 커밋 메시지 작성 (Validator가 호출)
      │
      ├── commands/
      │   ├── sprint-dev.md           ← /sprint-dev {스프린트명}
      │   ├── status.md               ← /status — 현재 상태 요약
      │   ├── next.md                 ← /next — 다음 에이전트 안내
      │   ├── rollback.md             ← /rollback — PHASE 롤백
      │   ├── sprint-log.md           ← /sprint-log — 스프린트 종합 요약
      │   ├── debt.md                 ← /debt — Tech Debt 보고
      │   ├── branch.md               ← /branch — 브랜치 생성
      │   ├── prd.md                  ← /prd — PRD.md 생성
      │   └── resolve.md              ← /resolve {이슈번호} — Redmine Resolved 처리
      │
      ├── skills/
      │   ├── redmine/SKILL.md          ← Redmine 이슈 조회
      │   └── commit-format/SKILL.md    ← YSR 커밋 메시지 형식
      │
      └── rules/
          ├── encoding-critical.md    ← CP949 인코딩 보호 (항상 활성화)
          ├── coding-principles.md    ← Delphi/Pascal 코딩 원칙
          ├── delphi2007-patterns.md  ← Delphi 2007 구현 패턴 모음 (명시적 Read 필요)
          ├── sprint-workflow.md      ← Sprint 워크플로우 보완 규칙
          └── dev-process.md          ← 개발 프로세스 정책 문서
