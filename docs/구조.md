● claude-code-base/ (Java + Spring Boot 프로젝트 기준)
  │
  ├── docs/PRD.md                          ← [사용자 작성] 프로젝트 요구사항 문서
  │                                           (목적, 기능, 기술 스택, MVP 기준, 제약 조건)
  │
  ├── docs/STATUS.md                       ← [공유 상태 파일] 현재 PHASE 기록
  │
  ├── plan.md                              ← [자동 생성] PRD 분석 결과
  ├── CLAUDE.md                            ← [자동/수동 생성] 기술 스택, 빌드 명령, 코딩 원칙
  ├── CHANGELOG.md                         ← [자동 생성] 전체 변경 이력 (Validator 생성)
  │
  ├── src/                                 ← Spring Boot 소스 코드
  │   ├── main/
  │   │   ├── java/com/example/project/
  │   │   │   ├── ProjectApplication.java
  │   │   │   ├── config/                  ← Security, Cors, JPA 등 설정
  │   │   │   ├── controller/              ← REST Controller
  │   │   │   ├── service/                 ← 비즈니스 로직 (interface + impl)
  │   │   │   ├── repository/              ← Spring Data JPA Repository
  │   │   │   ├── domain/                  ← Entity 클래스 (단수형 네이밍)
  │   │   │   ├── dto/                     ← Request/Response DTO
  │   │   │   ├── exception/               ← 커스텀 예외 + GlobalExceptionHandler
  │   │   │   └── security/                ← Spring Security 설정 (인증/인가)
  │   │   └── resources/
  │   │       ├── application.yml          ← 공통 설정
  │   │       ├── application-local.yml    ← 로컬 개발 환경
  │   │       ├── application-test.yml     ← CI/CD 테스트 환경
  │   │       ├── application-prod.yml     ← 프로덕션 (시크릿은 환경변수 참조)
  │   │       └── db/migration/            ← Flyway 마이그레이션 (선택)
  │   └── test/
  │       └── java/com/example/project/
  │           ├── controller/              ← @WebMvcTest 슬라이스 테스트
  │           ├── service/                 ← @ExtendWith(MockitoExtension) 단위 테스트
  │           └── repository/             ← @DataJpaTest 레포지토리 테스트
  │
  ├── e2e/                                 ← Playwright E2E 테스트 (Node.js 별도 모듈)
  │   ├── playwright.config.ts             ← baseURL, timeout, retry 설정
  │   ├── package.json
  │   └── tests/
  │       └── *.spec.ts                    ← 핵심 사용자 시나리오
  │
  ├── Dockerfile                           ← Multi-stage 빌드 (JDK → JRE)
  ├── docker-compose.yml                   ← 로컬 개발용 (DB, Redis 등)
  ├── pom.xml (또는 build.gradle)
  │
  ├── .github/
  │   └── workflows/
  │       └── ci.yml                       ← CI/CD 파이프라인 (빌드→테스트→E2E→배포)
  │
  ├── sprints/                             ← [자동 생성] Orchestrator 초기화 시 생성
  │   ├── ROADMAP.md                       ← 전체 스프린트 로드맵
  │   ├── TECH_DEBT.md                     ← 누적 기술 부채 중앙 관리
  │   ├── sprint-01/
  │   │   ├── GOAL.md                      ← 구현 명세서 (Planner 생성)
  │   │   ├── DONE.md                      ← 완료 보고서 (Validator 생성)
  │   │   └── OUT_OF_SCOPE.md              ← 범위 외 발견사항
  │   └── sprint-02/
  │       └── ...
  │
  └── .claude/
      ├── settings.json                    ← Claude Code 권한 설정
      │
      ├── agents/
      │   ├── orchestrator.md              ← PHASE 1~4.5: PRD 분석 → plan.md → ROADMAP.md
      │   ├── planner.md                   ← PHASE 5: GOAL.md 작성 (Spring Boot 산출물 기준)
      │   ├── implementer.md               ← PHASE 6: 구현 (Java 패턴 준수)
      │   ├── validator.md                 ← PHASE 7~10: 빌드→테스트→E2E→PR
      │   ├── hotfix-close.md              ← 핫픽스 마무리
      │   └── deploy-prod.md               ← 프로덕션 배포 (CI/CD 연동)
      │
      ├── commands/
      │   ├── sprint-dev.md                ← /sprint-dev {스프린트명}
      │   ├── status.md                    ← /status — 현재 상태 요약
      │   ├── next.md                      ← /next — 다음 에이전트 안내
      │   ├── rollback.md                  ← /rollback — PHASE 롤백
      │   ├── sprint-log.md                ← /sprint-log — 스프린트 종합 요약
      │   └── debt.md                      ← /debt — Tech Debt 보고
      │
      └── rules/
          ├── sprint-workflow.md           ← 스프린트/핫픽스 워크플로우 규칙
          ├── coding-principles.md         ← Java + Spring Boot 코딩 원칙
          ├── cicd.md                      ← CI/CD 파이프라인 규칙 (GitHub Actions)
          └── dev-process.md               ← 개발 프로세스 정책 문서
